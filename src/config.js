const EXTENSION_ID = 'com.danielayvar.obr-draw-steel'
export const APP_VERSION = '0.6.0'

export const SCENE_METADATA_ID = `${EXTENSION_ID}/scene-metadata-id`
export const GROUP_METADATA_ID = `${EXTENSION_ID}/group-metadata-id`

// Metadata for deciding the group and group id of a token
export const TOKEN_CONTEXT_MENU_ID = `${EXTENSION_ID}/token-toggle-id`
export const TOKEN_METADATA_ID = `${EXTENSION_ID}/token-toggle-metadata-id`

// Size of the app
export const APP_WIDTH = 350
export const APP_HEIGHT = 150

// Group options id for popover
export const GROUP_OPTIONS_POPOVER_ID = `${EXTENSION_ID}/group-options-popover-id`

// Size of group options
export const GROUP_OPTIONS_WIDTH = 200
export const GROUP_OPTIONS_HEIGHT = 335
export const MAX_TURN_COUNT = 3
export const MAX_REACTION_COUNT = 1

// Token options id for popover
export const TOKEN_OPTIONS_POPOVER_ID = `${EXTENSION_ID}/token-options-popover-id`

// Size of group options
export const TOKEN_OPTIONS_WIDTH = 200
export const TOKEN_OPTIONS_HEIGHT = 130

// Settings id for modal
export const SETTINGS_MODAL_ID = `${EXTENSION_ID}/settings-modal-id`

export const SETTINGS_WIDTH = 400
export const SETTINGS_HEIGHT = 600

export const PLACE_HOLDER_TOKEN_IMAGE = './icons/token_placeholder.svg'
